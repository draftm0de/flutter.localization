library draftmode_localization;
/// Public entry point that re-exports the generated DraftMode localization
/// delegate so consumers only depend on `package:draftmode_localization`.
export 'l10n/app_localizations.dart';
